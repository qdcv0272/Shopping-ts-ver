// shopsort module: animation removed per request. keep a no-op initializer so module remains safe to import.
export function initShopSort(): void {
  // no-op by design — GSAP animations removed for dropdown
  return;
}

export default initShopSort;
